export interface Person {
    id?: number;
    text: string;
    reminder: boolean;
    day: string;
    // nombre: string;
    // created_at?: Date;
    // apellido: string;
    // email:string;
    // foto?:null;
}